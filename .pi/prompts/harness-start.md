---
description: Start a local harness task packet
---

Create a task packet for this request using the `harness_create_task` tool.

Request:

$ARGUMENTS

After creating it, read back the packet path and summarize the stop conditions.

